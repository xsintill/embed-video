//Next import is needed to prevent Unknown provider: <ServiceName>ServiceProvider <- <ServiceName>Service and still move the consts to it's own file
import './video-embed.ui';

/**
 *  Container component llq-video-embed
 */
export class VideoEmbedContainer {
    /**
     *  Field with value used for registering the component llq-video-embed
     */
    public static componentName = 'llqVideoEmbed';
}
